{
}

rule scan_newline[T : @lexbuf.IStringLexbuf](lexbuf : T) -> Int {
  parse {
    ('\r'? '\n' | '\r') as t => {
      $startpos(t)
    }
    _ => { scan_newline(lexbuf) }
    "" => { -1 }
  }
}

{
}
